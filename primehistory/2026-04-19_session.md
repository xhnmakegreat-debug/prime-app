# Session Log — 2026-04-19

## 更新内容

### 1. 应用图标替换
- 源图：`C:\Users\xueha\Downloads\ChatGPT Image 2026年4月19日 02_48_20.png`（圆角蓝紫点阵箭头风格）
- 使用 `electron-icon-maker` 生成 `public/icon.ico`（Windows）、`public/icon.icns`（macOS）、`public/icon.png`（Linux 512×512）

### 2. GLM 默认模型更新
- `src/lib/providers/glm.js`：默认模型由 `glm-4-flash` 改为 `glm-4v`（支持视觉输入）

### 3. 提示词语言自适应
- `src/lib/prompts/promptB.js`、`promptC.js`、`promptD.js` 各加一行规则：输出语言跟随用户输入语言
- `promptA.js` 不修改（问卷引导阶段保持中文）

### 4. 日志附件功能（图片 / PDF / Word → LLM）

**新增文件：**
- `src/lib/attachments.js`：文件解析工具（base64 图片、pdfjs-dist 提取 PDF 文本、mammoth 提取 Word 文本）

**修改文件：**
- `src/lib/llm.js`：`chat()` 新增 `attachments = []` 参数
- `src/lib/providers/anthropic.js`：构建 Anthropic vision 格式（image source base64），有图片时自动切换 `claude-3-5-sonnet-20241022`
- `src/lib/providers/openai.js`：构建 OpenAI vision 格式（image_url + base64）
- `src/lib/providers/glm.js`：构建 GLM-4V vision 格式（同 OpenAI，image_url）
- `src/lib/providers/ollama.js`：图片附件忽略，文档附件作为文本注入
- `src/pages/DailyJournal.jsx`：新增附件上传 UI（拖拽 + 点击）、附件列表、分析结果展示；评分时先调用 `chat()` 分析附件，再嵌入 (text + analysis) 计算 ΔP

**数据结构扩展（localStorage journal entry）：**
```javascript
{
  // 原有字段不变，新增：
  attachments: [{ id, name, type, mime, size, uploadedAt, data }],  // data 含 base64/提取文本
  attachment_analysis: string | null
}
```
> ⚠ `data` 字段（base64 图片）存在 localStorage，大文件多时可能接近 5MB 限制。后期如需完整文件存档，将 data 移至 IndexedDB，保留 `dataRef` 字段指向即可。

**新增依赖：**
- `pdfjs-dist@5.6.205`
- `mammoth@1.12.0`
- `electron-icon-maker`（devDep，仅用于图标生成）
- `png-to-ico`（devDep，副产品）
- `sharp`（devDep，副产品）

## 遗留事项

- **i18n（中英双语）**：计划已定，未实施。约 450-500 条字符串，需安装 `react-i18next`，新建 `src/locales/zh.json` 和 `en.json`，语言偏好存 `prime:settings`
- **大文件 / 多附件 localStorage 溢出风险**：Demo 阶段可接受，后期需 IndexedDB 方案
- **pdfjs-dist chunk 大（2.2MB）**：可后续用 `manualChunks` 拆分优化包体积
