# .env.example — 仅供参考，不再用于注入配置
#
# 所有 API Key 和供应商配置请在应用内「设置」页面填写。
# 配置数据存储在本地 localStorage，不会写入代码或打包产物。
#
# 如需覆盖（仅开发调试用途），可创建 .env.local（已 gitignore）：
# VITE_PROVIDER=glm
# VITE_GLM_API_KEY=your_key_here
