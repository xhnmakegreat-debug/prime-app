# 选择 LLM 供应商：glm | anthropic | ollama
VITE_PROVIDER=glm

# 各供应商配置（也可在应用内 Setup 页配置，优先级更高）
VITE_GLM_API_KEY=
VITE_ANTHROPIC_API_KEY=
VITE_VOYAGE_API_KEY=
VITE_OLLAMA_BASE_URL=http://localhost:11434

# P 值计算混合权重，默认 0.7（0.1 ~ 0.9）
VITE_ALPHA=0.7
